node_modules/
# Ignore all .env files
*.env
# z generated files - Ignore all files starting with "z"
z*
# Root config folder - Ignore this directory at the root due to leading slash
# Removing the slash would ignore "artifacts" directories at all depths 
/artifacts/
